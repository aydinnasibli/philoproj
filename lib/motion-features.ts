// Async-loaded framer-motion feature set for LazyMotion.
// domAnimation (~17KB) covers animate, initial, exit, transition, and variants —
// sufficient for all m.* usage in this app (no drag, layout, or gesture features needed).
export { domAnimation as default } from "framer-motion";
